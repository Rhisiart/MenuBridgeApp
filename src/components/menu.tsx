import { FC } from "react";
import { View } from "react-native";

interface IProps {
    name: string,
}

const Menu: FC<IProps> = ({name}) => {
    return (
        <View>
            
        </View>
    )
}

export default Menu;